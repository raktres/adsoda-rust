
// #![warn(clippy::all, clippy::pedantic)]
pub mod adsoda;
// use adsoda::halfspace::*;



fn main() {
    
   // let result = matrix_multiply(&m1, &m2);
    // println!("{:?}", result); // Output: [50.0, 122.0]
/* 
    let a = vec![10.0, 20.0, 30.0];
    let b = vec![5.0, 15.0, 25.0];

    let result = vector_subtract(&a, &b);
    println!("{:?}", result); // Output: [5.0, 5.0, 5.0]
*/
    /*
    println!("combinaisons");
    let l = 10;
    let a = 4;
    let b = 5;
    let result = among_index(l, a, b);
    for comb in result {
        println!("{:?}", comb);
    }
    */
    // //

    // =========================
    /* 
    let points_array1 = vec![
        vec![1.0, 2.0, 3.0],
        vec![4.0, 5.0, 6.0],
        vec![7.0, 8.0, 9.0],
    ];

    let points_array2 = vec![
        vec![1.0, 0.0, 0.0],
        vec![0.0, 1.0, 0.0],
        vec![0.0, 0.0, 1.0],
    ];

    if let Some(normal1) = halfspaces_findnormal(points_array1) {
        println!("Normal vector for points_array1: {:?}", normal1);
    } else {
        println!("No normal vector found for points_array1");
    }

    if let Some(normal2) = halfspaces_findnormal(points_array2) {
        println!("Normal vector for points_array2: {:?}", normal2);
    } else {
        println!("No normal vector found for points_array2");
    }

    */
    // ============
    /* 
    let matrix = vec![
        vec![1.0, 2.0, 3.0, 4.0],
        vec![5.0, 6.0, 7.0, 8.0],
        vec![9.0, 10.0, 11.0, 12.0],
    ];

    if let Some(result) = matrix_solution(matrix) {
        println!("Result: {:?}", result);
    } else {
        println!("No solution found");
    }
*/
  //  println!("{:?}", among_index(4, 2, 3));
}
